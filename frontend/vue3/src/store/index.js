import { createStore } from "vuex";
import { category } from './modules/category'

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        category: category
    }
});